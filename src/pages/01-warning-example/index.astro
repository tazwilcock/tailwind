---
import Layout from "../../layouts/Layout.astro"
import HomeBtn from "../../components/HomeBtn.astro"
---

<Layout title="Vanilla CSS vs Tailwind CSS">
   <div class="pl-5 pt-5">
      <HomeBtn />
   </div>
   <div class="text-center pb-10">
      <h3 class="text-2xl">Alert Box</h3>
      <p>Vanilla CSS vs Tailwind CSS</p>
   </div>

   <!-- VANILLA CSS VERSION -->

   <p class="text-center pb-3 underline underline-offset-2 text-2xl font-bold">
      Vanilla CSS
   </p>

   <div class="flex-wrapper">
      <div class="alert">
         <div class="alert-logo-wrap">
            <img class="alert-logo" src="/warning.svg" alt="alert" />
         </div>
         <div class="alert-body">
            <h4 class="alert-title">Are You Sure?</h4>
            <p class="alert-message">You are about to delete a post</p>
         </div>
      </div>
   </div>

   <img
      class="max-w-sm md:max-w-md mx-auto my-5 rounded-2xl"
      src="/warning-styles.png"
      alt="code for warning pop up"
   />

   <!-- TAILWIND VERSION -->

   <p
      class="text-center text-2xl font-bold pt-10 pb-6 underline underline-offset-2"
   >
      Tailwind CSS
   </p>

   <div class="flex justify-center">
      <div
         class="flex items-center mx-4 p-6 bg-[#0F8777] max-w-sm rounded-xl shadow-lg space-x-4"
      >
         <img class="h-14 w-14" src="/warning.svg" alt="alert" />

         <div>
            <h4 class="text-xl text-black font-medium">Are You Sure?</h4>
            <p class="text-slate-700">You are about to delete a post</p>
         </div>
      </div>
   </div>

   <p class="text-center py-5">
      Wrapper Div with 2 classes: flex, justify-center
   </p>
   <div class="flex flex-col items-center pb-5">
      <p>Parent Div that holds icon, title, and p tag:</p>
      <p>flex</p>
      <p>items-center</p>
      <p>mx-4 (1rem left and right)</p>
      <p>p-6 (1.5rem all sides)</p>
      <p>bg-[#0F8777] (background color)</p>
      <p>max-w-sm (max width 24rem)</p>
      <p>rounded-xl (border radius 0.75rem)</p>
      <p>shadow-lg (large box-shadow)</p>
      <p class="pb-5">space-x-4 (16px between items on x-axis)</p>
      <p>(Children)</p>
      <div class="pb-3">img src="warning.svg" with 2 classes h-14 w-14</div>
      <div>Div that hold the h4 and p tags:</div>
      <div>h4 with 3 classes - text-xl text-black font-medium</div>
      <div>p tag with 1 class - text-slate-700</div>
   </div>
</Layout>

<style>
   .flex-wrapper {
      display: flex;
      justify-content: center;
   }

   .alert {
      display: flex;
      max-width: 24rem;
      margin: 0 1rem;
      padding: 1.5rem;
      border-radius: 0.5rem;
      background-color: #fff;
      box-shadow:
         0 20px 25px -5px rgba(0, 0, 0, 0.1),
         0 10px 10px -5px rgba(0, 0, 0, 0.04);
   }
   .alert-logo-wrap {
      flex-shrink: 0;
   }
   .alert-logo {
      height: 3rem;
      width: 3rem;
   }
   .alert-body {
      margin-left: 1.5rem;
      padding-top: 0.25rem;
   }
   .alert-title {
      color: #1a202c;
      font-size: 1.25rem;
      line-height: 1.25;
      font-weight: 500;
   }
   .alert-message {
      color: #718096;
      font-size: 1rem;
      line-height: 1.5;
   }
</style>
